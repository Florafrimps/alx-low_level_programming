#include <stdlib.h>
#include <time.h>
/**
*main -  determine if a random number is positive, negative or zero 
*
*Return: 0 on success
*/
int main(void)
{
0	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* if (n > 0)
        printf("is positive\n");
    else if (n == 0)
        printf("is zero\n");
    else
        printf("is negative\n"); */
	return (0);
}
